{
  "name": "grimsi.de",
  "version": "1.0.0",
  "description": "my personal website",
  "main": "index.html",
  "directories": {
    "lib": "lib"
  },
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.15.2",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "compile-scss-watch": "sass --watch scss/main.scss:app/main.css --style compressed",
    "compile-scss": "sass --update scss/main.scss:app/main.css --style compressed",
    "compile-typescript": "tsc --outFile app/main.js",
    "start-http-server": "http-server",
    "start": "npm run compile-scss && npm run compile-typescript && npm run start-http-server"
  },
  "author": "",
  "license": "DBADL"
}
